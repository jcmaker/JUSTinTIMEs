import React from "react";

const Projects = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "400px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <span>Page is Not Ready Yet!</span>
    </div>
  );
};

export default Projects;
